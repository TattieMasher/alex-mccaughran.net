$(document).ready(function() {
    // Initialize the Slick Slider
    $('.code-block-slider').slick({
        arrows: true,
        draggable: false,
        dots: true,
        infinite: true
    });
});