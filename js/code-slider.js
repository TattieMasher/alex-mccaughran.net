$(document).ready(function() {
    // Initialise the Slick Slider
    $('.code-block-slider').slick({
        arrows: true,
        draggable: false,
        dots: true,
        infinite: true
    });
});